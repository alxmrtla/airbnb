{
  "name": "airbnb_martynov",
  "version": "1.0.0",
  "description": "Airbnb clone project",
  "main": "index.js",
  "scripts": {
    "start": "npm run migrate && npm start --prefix backend",
    "migrate": "npx sequelize-cli db:migrate --env production",
    "install:backend": "npm install --prefix backend",
    "build": "npm run install:backend && npm run build --prefix backend && npm run migrate --prefix backend && npm run seed --prefix backend",
    "seed": "npx sequelize-cli db:seed:all --env production"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
